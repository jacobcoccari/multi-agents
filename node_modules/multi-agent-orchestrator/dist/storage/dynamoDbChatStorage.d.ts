import { ChatStorage } from "./chatStorage";
import { ConversationMessage } from "../types";
export declare class DynamoDbChatStorage extends ChatStorage {
    private tableName;
    private docClient;
    private ttlKey;
    private ttlDuration;
    constructor(tableName: string, region: string, ttlKey?: string, ttlDuration?: number);
    saveChatMessage(userId: string, sessionId: string, agentId: string, newMessage: ConversationMessage, maxHistorySize?: number): Promise<ConversationMessage[]>;
    fetchChat(userId: string, sessionId: string, agentId: string): Promise<ConversationMessage[]>;
    fetchAllChats(userId: string, sessionId: string): Promise<ConversationMessage[]>;
    private generateKey;
    private removeTimestamps;
}
