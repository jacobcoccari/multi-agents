import { ConversationMessage } from "../types";
import { Agent, AgentOptions } from "./agent";
export interface LambdaAgentOptions extends AgentOptions {
    functionName: string;
    functionRegion: string;
    inputPayloadEncoder?: (inputText: string, ...additionalParams: any) => any;
    outputPayloadDecoder?: (response: any) => ConversationMessage;
}
export declare class LambdaAgent extends Agent {
    private options;
    private lambdaClient;
    constructor(options: LambdaAgentOptions);
    private defaultInputPayloadEncoder;
    private defaultOutputPayloaderDecoder;
    processRequest(inputText: string, userId: string, sessionId: string, chatHistory: ConversationMessage[], additionalParams?: Record<string, string>): Promise<ConversationMessage>;
}
