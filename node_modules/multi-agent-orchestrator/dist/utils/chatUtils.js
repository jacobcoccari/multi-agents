"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveConversationExchange = saveConversationExchange;
const types_1 = require("../types");
async function saveConversationExchange(userInput, agentResponse, storage, userId, sessionId, agentId, maxHistorySize) {
    const userInputObj = {
        role: types_1.ParticipantRole.USER,
        content: [{ text: userInput }],
    };
    await storage.saveChatMessage(userId, sessionId, agentId, userInputObj, maxHistorySize);
    const agentResponseObj = {
        role: types_1.ParticipantRole.ASSISTANT,
        content: [{ text: agentResponse }],
    };
    await storage.saveChatMessage(userId, sessionId, agentId, agentResponseObj, maxHistorySize);
}
//# sourceMappingURL=chatUtils.js.map