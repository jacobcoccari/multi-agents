import { ChatStorage } from '../storage/chatStorage';
export declare function saveConversationExchange(userInput: string, agentResponse: string, storage: ChatStorage, userId: string, sessionId: string, agentId: string, maxHistorySize?: number): Promise<void>;
